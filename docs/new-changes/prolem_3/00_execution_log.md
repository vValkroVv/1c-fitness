# Лог выполнения проблемы 3: импорт услуг

Дата начала: `2026-06-23`.

Цель: выполнить `docs/new-changes/03_services_import_plan.md` до итоговых XLSX
в `output/20251115_0800_fix_owner_new_import/`.

Фокус: максимально точно собрать текущие активные услуги на срез
`2026-05-25 08:00` для клиентов из финального `import_заявки`.

## Принятые входы

- Источник клиентов:
  `output/20251115_0800_fix_owner/fitbase_active_clients_import_zayavki_20260525_0800_all_funnels.xlsx`.
- SQL-база:
  `FitnessRestored_20260523_macos`.
- SQL runtime:
  `mssql-fitness-2022`.
- Список услуг:
  `new-changes/Услуги список нужных.xlsx`, 51 строка.

## Ход работ

- Создана папка отчетности `docs/new-changes/prolem_3/`.
- Проверены шаблоны:
  - `new-changes/Импорт_услуги_клиентов.xlsx`: 16 рабочих колонок.
  - `new-changes/Импорт_шаблоны_услуг.xlsx`: 9 рабочих колонок.
- Проверена доступность SQL-базы: база онлайн, staging-схема `fitbase_part2`
  доступна.
- Найден основной источник продаж услуг:
  `dbo._Document154_VT1137._Fld1146RRef -> dbo._Reference72`.
- Найден клиент продажи услуги:
  `dbo._Document154._Fld1119RRef -> dbo._Reference64`.
- Для пакетных услуг найдена связь строки продажи с экземпляром услуги:
  `dbo._Document154_VT1137._Fld1148_RTRef = 0x000000A3` и
  `_Fld1148_RRRef -> dbo._Document163`.
- Для пакетных услуг найден остаток посещений:
  `dbo._AccumRg3336._Fld3337_RRRef = Document163._IDRRef`,
  баланс = приход минус расход по `_Fld3339` на cutoff.
- Собран SQL staging `fitbase_part2.services_import_facts`:
  - всего строк продаж услуг до cutoff: `50 221`;
  - услуг из 51 с продажами в базе: `44`;
  - активных строк до фильтра финального XLSX: `360`;
  - активных названий услуг до фильтра финального XLSX: `4`.
- Запущен воспроизводимый пайплайн:
  `scripts/32_build_services_import_outputs.sh`.
- Итоговая сборка:
  - клиентский XLSX: `529` строк;
  - активные строки среди финальных клиентов: `352`;
  - исторические fallback-строки: `176`;
  - исторические fallback-строки вне `import_заявки`: `1`;
  - шаблоны услуг: `51` строк;
  - услуг с клиентскими строками: `44`;
  - услуг только в шаблонах: `7`.
- Валидация `scripts/24_validate_services_import_xlsx.py`: `PASS`.
- По просьбе заказчика historical fallback расширен: если по услуге нет строк
  среди клиентов финального `import_заявки`, берем реальные исторические
  продажи вне `import_заявки`. Так добавлена `Утеря валика`.
