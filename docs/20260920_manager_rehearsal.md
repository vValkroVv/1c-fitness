# Репетиция смены менеджеров на принятой июньской поставке

Дата проверки: 20 сентября 2026 года. Результат: **PASS**.

Текущие сборщик `17_build_part2_combined_xlsx.py` и валидатор
`18_validate_combined_single_stage_outputs.py` запущены на копии принятого
июньского staging. Срез сохранён: **2026-06-30 23:27:03**. Эта проверка
отделяет смену менеджеров от переноса даты актуальности и новой базы.
SQL не выполнялся; работа не затрагивала параллельную репетицию `+1 день`.

## Источники и отдельный рабочий каталог

- Принятый staging, отчёты и CSV:
  `end-to-end-xlsx/work/20260630_funnel_labels_20260820/owner/`.
- Контрольная поставка:
  `output/20260630_delivery_funnel_labels_20260820/`.
- Рабочая копия проверки:
  `end-to-end-xlsx/work/20260920_rehearsal_managers/owner/`.
- Рабочий каталог исключён из Git правилом `work/*` автономного пакета.
- SHA-256 исходного `staging/final_funnel_clients.csv`:
  `3c7e4415a56b4eed0827154649ac4e00b272f2ec8ab7366e9cbf6b017d836e39`.

SHA-256 всех файлов принятого owner-каталога и контрольной поставки проверены
до и после работы: исходники и принятые XLSX не изменились.

## Что было запущено

Использован Python 3.14.5 из `end-to-end-xlsx/.venv/bin/python`.
Сначала выполнен действующий сборщик 17, затем действующий валидатор 18.
Оба завершились с кодом `0`; валидатор выдал `PASS`, ошибок `0`.

Параметры совпадают с `Pipeline.main_xlsx()`:

```text
--cutoff-date 2026-06-30
--date-stamp 20260630
--managers-config config/managers_by_club.yml
--branches-config config/branches_by_club.yml
--main-require-phone-for-new-applications
--main-transfer-new-applications-to-memberships
--cards-funnel-filter "Действующие клиенты"
--dedupe-by-phone-keep-latest-subscription
```

Сборщику дополнительно передан
`--fitbase-label-mode customer_20260520_single_stage`. Использованы штатные
шаблоны `templates/import_zayavki.xlsx` и `templates/plastic_cards.xlsx`.
Все выходные пути направлены в отдельную рабочую копию.

В рабочем каталоге сохранены точные команды с абсолютными путями
(`commands.json`), исполняемый сценарий (`rehearse.py`), логи
`logs/build.log`, `logs/validate.log` и машинный результат `comparison.json`.

## Полное сравнение с принятой поставкой

| Проверка | Результат |
| --- | ---: |
| Строк в исходном staging | 73 292 |
| Заявок в обоих XLSX | 39 550 |
| Действующих клиентов | 11 024 |
| Клиентов реактивации | 28 526 |
| Изменённых ячеек `manager` | 35 792 |
| Изменённых ячеек вне `manager` | **0** |
| Строк пластиковых карт в обоих XLSX | 11 024 |
| Изменённых ячеек файла карт | **0** |

Сопоставлены **все строки и все ячейки**, включая типы значений, заголовки
и порядок строк. Набор ID клиентов совпадает; каждый ID в заявках уникален.
Филиалы, телефоны, даты, воронки, этапы и номера карт сохранены полностью.

Дополнительно сравнены XML-части XLSX. Дерево листа заявок совпадает после
исключения только содержимого ячеек `I3:I39552` (`manager`); дерево листа
карт совпадает полностью. Остальные XML-части, включая стили и структуру
книг, совпадают; при сравнении исключена только служебная дата сохранения
из `docProps/core.xml`. Поэтому различие SHA-256 целого XLSX не означает
изменения его данных или оформления.

## Фактическое распределение

| Менеджер | Клиентов в итоговом XLSX |
| --- | ---: |
| Пеуна Анастасия Ивановна | 13 161 |
| Пилия Анастасия Артуровна | 13 140 |
| Ефремова Алена | 13 249 |
| **Всего** | **39 550** |

В итоговом файле присутствуют только эти три имени. Для каждой строки
валидатор пересчитал ожидаемое назначение по `client_id` из общего пула.
Требования одинакового количества клиентов у каждого менеджера нет.

## Существующие предупреждения качества исходных данных

Валидатор вывел семь предупреждений: 25 позиций классификации требуют
бизнес-проверки; 5 678 новых заявок без телефона исключены; 25 676 отказников
направлены в импорт абонементов; удалены 2 388 дублей по телефону; в заявках
сохранены 408 действующих клиентов/клиентов реактивации без телефона;
в полном staging 22 707 клиентов без выбранной карты и 13 169 клиентов
с несколькими кандидатами абонементов. Эти предупреждения не являются
изменениями смены менеджеров: сравнение с принятой поставкой показало
нулевое изменение остальных данных.

## Границы проверки

Репетиция подтверждает смену менеджеров в файле заявок и сохранность файла
карт на прежнем срезе. Абонементы, услуги и фотографии в этой отдельной
проверке не пересобирались; их проверяет полный прогон с новой логикой
даты актуальности.
