# Текущее состояние payment_type и price=0

Срез: `2026-05-25 08:00`.

Этот файл - единая рабочая сводка по проблемам `payment_type` и `price=0`.
Разрозненные файлы `05`-`09` свернуты сюда.

Актуальные XLSX:

- `output/20251115_0800_fix_owner_new_import/fitbase_import_abonementy_clientov_20260525_0800.xlsx`
- `output/20251115_0800_fix_owner_new_import/fitbase_import_shablony_abonementov_20260525_0800.xlsx`

XLSX для ручной проверки 25 примеров:

- `output/20251115_0800_fix_owner_new_import/payment_price_manual_review_examples_20260525_0800.xlsx`
- ответ от бизнеса:
  `output/20251115_0800_fix_owner_new_import/payment_price_manual_review_examples_20260525_0800-with-answers.xlsx`

Воспроизводимый запуск основной выгрузки:

```bash
scripts/31_build_membership_import_outputs.sh
```

Проверка после последней перегенерации: `PASS`.

Последнее событие по `payment_type/price`: получены ручные ответы по
`membership_import_representative_30_examples_20260525_0800-with-answers.xlsx`.
Owner-change по `Смена владельца подарочной карты` уже исправлен в основной
выгрузке. После разбора Поповой добавлено strict-исключение
`active full сейчас + следующий/later Контакт с клиентом`: `4` строки удалены
из основного XLSX.

Ручные ответы оставили два контрольных слоя:

- было `327` строк `price>0`, платеж не найден,
  `type_of_payment=наличные`; после strict-исключения осталось `325`;
- исходные `207` строк `price=0`, direct-платеж найден,
  `type_of_payment` оставлен, сокращены до `61`: внедрены правила для `4`
  direct-строк `Абонемент НЕДЕЛЯ САЙТ` и `176` historical direct-refund строк
  со связанным `Document131`. Открытыми остались `44` active full, `15`
  historical full без refund и `2` строки `Абонемент Неделя Фитнес` с payment
  на строке `Доплата`.

Детальный разбор:

```text
docs/new-changes/prolem_2/07_representative_30_manual_answers_followup.md
docs/new-changes/prolem_2/09_no_payment_cash_active_overlap_deep_dive.md
docs/new-changes/prolem_2/10_zero_price_direct_payment_deep_dive.md
```

## Верхняя сводка

Общий объем:

| метрика | rows |
|---|---:|
| всего строк абонементов | 99 383 |
| строк шаблонов | 114 |
| дублей `contract_id` | 0 |
| отсутствующих шаблонов по `contract_name` | 0 |
| исключено по `exclude_active_later_contact_full` | 4 |
| `payment_type = безналичные` | 65 383 |
| `payment_type = наличные` | 3 266 |
| `payment_type = сбп` | 1 449 |
| `payment_type` пустой | 29 285 |
| `price=0` | 29 346 |
| `price>0` | 70 037 |

Принятые бизнес-исключения:

| исключение | rows | правило |
|---|---:|---|
| старый 2018 full/gift пласт | 17 129 | `price=0`, оплачено `0`, остаток `0`, `type_of_payment` пустой |
| бесплатные/пробные | 4 958 | `price=0`, оплачено `0`, остаток `0`, `type_of_payment` пустой |
| подтвержденные бесплатные недели после ручной проверки | 3 626 | `НЕДЕЛЯ САЙТ` и `НЕДЕЛЯ ФИТНЕСА БЕСПЛАТНО` только при `price=0` и не-direct |
| `price=0` + fallback-платеж | 1 792 | `type_of_payment` очищен, цена/оплата остаются `0` |
| full `price=0` без платежа | 1 312 | ввод начальных остатков/корпоративный клиент/модификатор, цену не восстанавливаем автоматически |
| `price=0` + платеж не найден, остаточные короткие/субаренда | 288 | `type_of_payment` пустой, цену не восстанавливаем |
| historical `price=0` + direct + `Document131` refund | 176 | `type_of_payment` очищен; строка не считается обычной оплаченной продажей |
| direct `Абонемент НЕДЕЛЯ САЙТ` с нулевой строкой продажи | 4 | `type_of_payment` очищен; payment относится к другой строке sale doc |
| всего бизнес-исключений | 29 285 | больше не считаем ошибками |

Остаток без бизнес-исключений:

| остаточная проблема | rows |
|---|---:|
| строк без бизнес-исключений всего | 70 244 |
| `payment_type` пустой | 0 |
| `price=0` | 61 |
| одновременно `payment_type` пустой и `price=0` | 0 |
| открытый контрольный слой `price=0` + direct-платеж + `payment_type` заполнен | 61 |

## Новое правило по техническим raw method

По бизнес-подтверждению эти raw method маппятся в `безналичные`:

- `Для ошибок, атол 30Ф,Кульманова А.В. ИП`
- `Для ошибок, Кульманова А.В. ИП`
- `БАР ИП Иконников Андрей Анатольевич`

Эффект:

Промежуточный эффект этого отдельного правила до внедрения ручных ответов
бизнеса:

- кейс `price>0 + пустой payment_type + raw method технический` закрыт;
- `3 128` строк больше не требуют ручной проверки по `payment_type`;
- всего в non-business остатке замаплено `3 240` строк: `3 128` с `price>0`
  и `112` с `price=0`;
- `payment_type` пустой снизился с `32 698` до `29 458`;
- `безналичные` выросли с `65 214` до `68 454`.

Разбивка raw method после маппинга:

| raw method | rows total | non-business -> безналичные | business override -> blank | price=0 | price>0 |
|---|---:|---:|---:|---:|---:|
| Для ошибок, атол 30Ф,Кульманова А.В. ИП | 2 354 | 2 353 | 1 | 79 | 2 275 |
| Для ошибок, Кульманова А.В. ИП | 870 | 862 | 8 | 18 | 852 |
| БАР ИП Иконников Андрей Анатольевич | 44 | 25 | 19 | 43 | 1 |

Пустые значения в business override остаются пустыми намеренно, потому что для
старого 2018 пласта и подтвержденных бесплатных пробников `type_of_payment`
должен быть пустой независимо от найденного raw method.

## Текущие основные кейсы

| группа | rows | статус |
|---|---:|---|
| `price=0` + пустой `payment_type` + платеж не найден | 288 | закрыт: оставить `price=0`, `type_of_payment` пустой |
| `price>0` + пустой `payment_type` + raw method технический | 0 | закрыт правилом `-> безналичные` |
| `price>0` + пустой `payment_type` + direct-платеж найден, raw method пустой | 0 | закрыт правилом `-> наличные` |
| `price=0` + `payment_type` уже замаплен через fallback | 1 741 | закрыт: очистить `type_of_payment` |
| `price=0` + `payment_type` уже замаплен через direct | 61 | открыт после safe-правок: `44` active full, `15` historical full без refund, `2` `Неделя Фитнес`/`Доплата` |
| `price>0` + пустой `payment_type` + платеж не найден | 325 | закрыт: поставить `наличные`; strict active/later `Контакт с клиентом` исключен |
| `price=0` + пустой `payment_type` + платеж найден, raw method пустой | 85 | закрыт: оставить пустым |

## Ручные примеры

Создан файл:

```text
output/20251115_0800_fix_owner_new_import/payment_price_manual_review_examples_20260525_0800.xlsx
```

Внутри:

- один лист `manual_review_25` - 25 строк для ручной проверки.

Структура колонок:

- `question_for_manual_check`
- `correct_price`
- `correct_payment_type`
- `comment`
- далее ровно поля итогового файла импорта абонементов:
  `contract_id`, `client_id`, `phone`, `client_fio`, `contract_name`, `card`,
  `duration`, `duration_type`, `create_date`, `payment_date`,
  `activation_date`, `end_date`, `freeze`, `guests`, `visits_left`, `price`,
  `amount_of_payments`, `payment_left`, `type_of_payment`, `manager`.

Диагностические поля в файл не добавлены, чтобы проверяющие видели только
вопрос, поля для ответа и текущую строку в формате итогового импорта.

Состав 25 примеров:

| group | rows | зачем включено |
|---|---:|---|
| `A_zero_week_site_no_payment` | 4 | `Абонемент НЕДЕЛЯ САЙТ`, все нули, платеж не найден |
| `B_zero_multicard12_no_payment` | 4 | `Абонемент МУЛЬТИКАРТА 12 месяцев`, `price=0`, платеж не найден |
| `B_zero_ultra12_no_payment` | 3 | `Абонемент УЛЬТРА 12 месяцев`, `price=0`, платеж не найден |
| `C_zero_week_site_with_fallback_payment` | 2 | `НЕДЕЛЯ САЙТ`, цена ноль, но способ оплаты подтянут fallback-платежом |
| `C_zero_full_with_fallback_payment` | 2 | full-абонементы, цена ноль, но найден платеж |
| `C_zero_trial_with_fallback_payment` | 1 | пробный/недельный ноль с fallback-платежом |
| `D_positive_direct_raw_empty_ultra12` | 3 | direct-платеж есть, сумма есть, raw method пустой |
| `D_positive_direct_raw_empty_multicard12` | 2 | direct-платеж есть, сумма есть, raw method пустой |
| `D_positive_direct_raw_empty_week_site_paid` | 2 | платный `НЕДЕЛЯ САЙТ`, direct-платеж есть, raw method пустой |
| `D_positive_direct_raw_empty_other_full` | 2 | другие full direct-платежи с пустым raw method |

Кейс `price>0 + technical raw method` в примеры не включен, потому что он уже
закрыт бизнес-правилом `-> безналичные`.

## Ответ бизнеса по 25 примерам

Файл с ответами:

```text
output/20251115_0800_fix_owner_new_import/payment_price_manual_review_examples_20260525_0800-with-answers.xlsx
```

Важно по структуре файла:

- колонки `correct_price`, `correct_payment_type`, `comment` остались пустыми;
- проверяющие добавили две последние колонки без заголовков;
- первая безымянная колонка содержит тип объяснения, вторая - комментарий.

Сводка ответов:

| ответ | строк | вывод для правил |
|---|---:|---|
| `Бесплатная неделя` / `цена 0` или `Статус "Бесплатно"` | 7 | нулевые недельные/бесплатные строки не нужно восстанавливать из платежа; `price=0`, оплачено `0`, `type_of_payment` пустой |
| `Ввод начальных остатков` / `Корпоративный клиент` | 7 | часть full-абонементов с `price=0` - не ошибка цены, а корпоративный/начальный остаток; нельзя массово восстанавливать цену из прайса |
| `Модификатор` / `Переоформление Ультра на Мульти` | 1 | full-абонемент с `price=0` может быть переоформлением, а не потерянной оплатой |
| `Платное изменение условий членства(модификатор)` / `С Ультра 3 на Ультра 12` | 1 | среди full-нулей с найденным fallback-платежом есть реальные платные модификаторы; нужен отдельный признак, простое очищение всех fallback-платежей неверно |
| `Оплата наличными через эммулятор` / `Не всю наличку фискализировали` | 9 | для direct-платежей с `price>0` и пустым raw method можно ставить `type_of_payment=наличные` |

Разложение ответов по нашим группам:

| группа | строк в примерах | ответ |
|---|---:|---|
| `price=0` + пустой `payment_type` + платеж не найден, `НЕДЕЛЯ САЙТ` | 4 | бесплатная неделя, цена `0` |
| `price=0` + пустой `payment_type` + платеж не найден, `МУЛЬТИКАРТА/УЛЬТРА 12` | 7 | 6 строк - ввод начальных остатков/корпоративный клиент; 1 строка - модификатор |
| `price=0` + найден fallback-платеж, недельные/бесплатные | 3 | бесплатная неделя/бесплатный статус; fallback-платеж не должен задавать `type_of_payment` |
| `price=0` + найден fallback-платеж, full | 2 | 1 строка - корпоративный/начальный остаток, 1 строка - платный модификатор |
| `price>0` + direct-платеж найден, raw method пустой | 9 | наличные через эмулятор |

Выводы из ответов, которые легли в текущую правку:

1. Закрыто `price>0 + direct payment + raw method пустой`: ставим
   `type_of_payment = наличные`, если найден direct-платеж
   `direct_doc152_vt1083_doc154_vt1137_doc163`, сумма платежа положительная,
   операция `Оплата от клиента`, а `matched_payment_method` пустой.
   Масштаб в текущей выгрузке: `2 941` строк.

2. Добавлены в явные бесплатные/нулевые исключения:
   `Абонемент НЕДЕЛЯ САЙТ` и
   `Абонемент НЕДЕЛЯ ФИТНЕСА БЕСПЛАТНО`, но только для `price=0`.
   Для них `price=0`, `amount_of_payments=0`, `payment_left=0`,
   `type_of_payment` пустой. Платные строки `НЕДЕЛЯ САЙТ` с `price>0`
   остаются платными; по ручной проверке для direct-платежей с пустым method
   это наличные через эмулятор.

3. Для `price=0` и `match_source=client_date_14_days` у подтвержденных
   бесплатных недельных строк очищать `type_of_payment`: ручные примеры
   показали, что fallback-платеж может быть соседней оплатой клиента и не
   относится к бесплатной неделе.

4. Для full-абонементов с `price=0` без платежа не восстанавливать цену
   автоматически из шаблона или соседних оплат. Ручные примеры показали
   корпоративных клиентов, ввод начальных остатков и модификаторы. Эти `1 312`
   строк переведены в отдельное допустимое исключение без изменения итоговых
   значений.

5. Для full-абонементов `price=0` с найденным fallback/direct-платежом
   автоматическое правило пока нельзя делать одним махом. В примерах есть
   оба варианта: ложный fallback корпоративного клиента и реальный платный
   модификатор `Ультра 3 -> Ультра 12`. Нужен дополнительный SQL-признак
   модификатора/переоформления или отдельная ручная выборка именно по этой
   подгруппе.

Исходный масштаб применения по всей выгрузке перед внедрением правил:

| слой | строк |
|---|---:|
| `price>0` + direct payment + пустой raw method | `2 941` |
| `Абонемент НЕДЕЛЯ САЙТ`, `price=0`, non-business | `3 244` |
| `Абонемент НЕДЕЛЯ ФИТНЕСА БЕСПЛАТНО`, `price=0`, non-business | `386` |
| из них `НЕДЕЛЯ САЙТ/ФИТНЕСА БЕСПЛАТНО` без платежа | `2 319` |
| из них `НЕДЕЛЯ САЙТ/ФИТНЕСА БЕСПЛАТНО` с fallback-платежом | `1 307` |
| `НЕДЕЛЯ САЙТ price=0` с direct-платежом | `4` |
| full `price=0` без платежа, non-business | `1 312` |
| full `price=0` с заполненным `type_of_payment`, non-business | `1 645` |

Обновление `2026-06-24`: `НЕДЕЛЯ САЙТ price=0` с direct-платежом (`4` строки)
проверен отдельно в `10_zero_price_direct_payment_deep_dive.md`. У всех `4`
целевая строка абонемента в `Document154_VT1137` равна `0`, а payment относится
к другой строке того же sale doc (`Перчатки`, годовой абонемент или подарок).
Это подтверждает бесплатность именно этих direct-строк; платные
`НЕДЕЛЯ САЙТ` с `price>0` этим правилом не затрагиваются.

## Финальные решения по остаткам

Дополнительные бизнес-решения внедрены в пайплайн:

- `288` строк `price=0`, `payment_type` пустой, платеж не найден:
  оставляем `price=0`, `amount_of_payments=0`, `payment_left=0`,
  `type_of_payment` пустой;
- `1 741` строк `price=0` с заполненным `payment_type` через fallback:
  очищаем `type_of_payment`;
- исходные `207` строк `price=0` с direct-платежом и заполненным
  `payment_type`: после deep-dive внедрены два safe-правила, открытый остаток
  уменьшен до `61`;
- `4` direct-строки `Абонемент НЕДЕЛЯ САЙТ` с нулевой целевой строкой продажи:
  очищаем `type_of_payment`;
- `176` historical zero-direct строк со связанным posted/unmarked
  `Document131` refund: очищаем `type_of_payment`; из них `142` раньше держали
  заполненный `payment_type`, `34` уже были blank из-за пустого raw method;
- было `327` строк `price>0`, `payment_type` пустой, платеж не найден:
  ставим `type_of_payment = наличные`;
- после strict-исключения active/later `Контакт с клиентом` в основном XLSX
  осталось `325` таких строк;
- `85` строк `price=0`, платеж найден, raw method пустой:
  оставляем `type_of_payment` пустым.

Техническая деталь по группе `85`: `51` строка из нее имеет
`matched_payment_match_source = client_date_14_days`, поэтому попала в общее
правило `business_zero_fallback_payment_type_blank`. Прежние `34` direct-строки
с пустым raw method оказались historical `Document131` refund-строками и теперь
помечаются как `business_historical_document131_refund_zero_direct_blank_payment`.

Итоговая проверка по взаимоисключающим группам:

| final_rule_group | rows | итог |
|---|---:|---|
| `A_zero_no_payment_blank_type_288` | 288 | blank `type_of_payment` |
| `B_zero_fallback_payment_type_blank_1792` | 1 792 | blank `type_of_payment` |
| `C_zero_direct_payment_type_kept_61` | 61 | после safe-правок остались `44` active full, `15` historical full без refund, `2` `Неделя Фитнес`/`Доплата` |
| `D_positive_no_payment_cash_325` | 325 | `type_of_payment = наличные`; `2` no-payment строки удалены strict-исключением |
| `E_zero_direct_document131_refund_blank_176` | 176 | blank `type_of_payment`, источник `Document131` |

Детальный построчный отчет по финальным правилам:

```text
output/20251115_0800_fix_owner_new_import/reports/payment_price_final_rule_check.csv
```

Важно: этот CSV был сформирован до strict-исключения active/later
`Контакт с клиентом`. Актуальные счетчики после удаления `4` строк находятся в:

```text
output/20251115_0800_fix_owner_new_import/reports/validation_report.md
output/20251115_0800_fix_owner_new_import/staging/membership_import_excluded_rows.csv
```

Репрезентативная ручная выборка `30` разных кейсов из всей финальной выгрузки:

```text
output/20251115_0800_fix_owner_new_import/membership_import_representative_30_examples_20260525_0800.xlsx
```

Важно: XLSX содержит только `20` колонок основного файла импорта, без
`question_for_manual_check`, `correct_price`, `correct_payment_type`,
`comment` и без staging-полей.

Описание состава выборки:

```text
docs/new-changes/prolem_2/representative_30_examples_20260525_0800.md
```

Состав выборки по блокам:

| блок проверки | rows |
|---|---:|
| обычные платные full-абонементы и спецпредложения | 4 |
| разные типы оплаты и payment-source правила | 4 |
| частичная оплата, рассрочка, owner-change | 4 |
| zero-price бизнес-правила | 12 |
| субаренда active/expired/unlimited | 4 |
| нестандартный продукт/короткий платный trial | 2 |

После этих правил нерешенных строк по группам с пустым `payment_type` нет.
Исходный контрольный слой `207` direct-строк с `price=0` после deep-dive и
safe-правок уменьшен до `61`: `44` active full, `15` historical full без
`Document131` refund и `2` строки `Неделя Фитнес` с payment на строке
`Доплата`.

Построчный отчет по активным `44` full-строкам:

```text
output/20251115_0800_fix_owner_new_import/reports/zero_price_direct_active_full_44_detail.csv
output/20251115_0800_fix_owner_new_import/reports/zero_price_direct_refund_link_audit.csv
output/20251115_0800_fix_owner_new_import/reports/zero_price_direct_refund_link_summary.csv
output/20251115_0800_fix_owner_new_import/reports/zero_price_direct_applied_overrides.csv
output/20251115_0800_fix_owner_new_import/reports/zero_price_direct_applied_overrides_summary.csv
output/20251115_0800_fix_owner_new_import/reports/zero_price_direct_trial_sale_context_audit.csv
output/20251115_0800_fix_owner_new_import/reports/zero_price_direct_trial_sale_context_summary.csv
```

## Историческое разложение до внедрения ответов бизнеса

### 1. price=0 + пустой payment_type + платеж не найден

Количество: `3 919`.

Что в данных:

- `price = 0` во всех строках;
- `amount_of_payments = 0` во всех строках;
- `matched_payment_ref` пустой;
- `matched_payment_amount = 0`;
- `matched_payment_method` пустой;
- `_money_source = zero_price` во всех строках.

Разбивка по типу продукта:

| product_class | rows | смысл |
|---|---:|---|
| `trial_or_guest` | 2 604 | в основном недельные/пробные нулевые строки |
| `full_subscription` | 1 312 | полноценные абонементы с нулевой ценой и без платежа |
| `unknown_review_required` | 3 | точечно, отдельно проверять |

Топ названий:

| contract_name | rows | что проверить |
|---|---:|---|
| Абонемент НЕДЕЛЯ САЙТ | 2 096 | может быть бесплатный слой, нужны ручные подтверждения |
| Абонемент МУЛЬТИКАРТА 12 месяцев | 665 | вероятная потеря цены/платежа, не похоже на бесплатный слой |
| Абонемент УЛЬТРА 12 месяцев | 476 | вероятная потеря цены/платежа, не похоже на бесплатный слой |
| Абонемент НЕДЕЛЯ ФИТНЕСА БЕСПЛАТНО | 223 | название бесплатное, но полный слой содержит платные строки |
| Абонемент Неделя Фитнес | 150 | смешанный платный/нулевой слой |
| Абонемент Неделя сайт 2023 | 135 | смешанный платный/нулевой слой |
| Абонемент УЛЬТРА 1 МЕСЯЦ БЕЗЛИМИТ | 50 | full-абонемент, нужно искать цену |
| Мультикарта на 6 месяцев | 35 | full-абонемент, нужно искать цену |

Главный вопрос для ручной проверки:

- какие нулевые `trial_or_guest` действительно бесплатные;
- где у `full_subscription` с `price=0` искать настоящую цену/платеж.

### 2. price>0 + пустой payment_type + raw method технический

Количество после нового правила: `0`.

Решение:

- `Для ошибок, атол 30Ф,Кульманова А.В. ИП -> безналичные`
- `Для ошибок, Кульманова А.В. ИП -> безналичные`
- `БАР ИП Иконников Андрей Анатольевич -> безналичные`

Этот кейс закрыт и не включен в XLSX для ручной проверки.

### 3. price=0 + payment_type уже замаплен

Количество: `3 156`.

Что в данных:

- `price = 0`;
- `amount_of_payments = 0`;
- `payment_type` уже заполнен:
  - `безналичные`: `3 117`
  - `сбп`: `39`
- основной источник матча платежа:
  - `client_date_14_days`: `2 949`
  - direct match: `207`

Топ raw method:

| raw method | rows |
|---|---:|
| Банк-эквайрер, менеджер | 1 075 |
| Эквайринг, Промышленная 1 | 426 |
| Банк-эквайрер, Столица1 | 332 |
| Эквайринг, Промышленная 2 | 217 |
| Эквайринг, Карельский1 | 200 |
| Банк-эквайрер, Гоголевский Рецепция 1 | 169 |
| Эквайринг, Столица2 | 168 |
| Банк-эквайрер, Гоголевский рецепция 2 | 122 |
| Банк-эквайрер, Кульманова А.В. ИП | 108 |
| Для ошибок, атол 30Ф,Кульманова А.В. ИП | 78 |

Топ названий:

| contract_name | rows | что проверить |
|---|---:|---|
| Абонемент НЕДЕЛЯ САЙТ | 1 049 | цена ноль, способ оплаты почти всегда fallback |
| Абонемент УЛЬТРА 12 месяцев | 693 | full-абонемент, часть может требовать восстановления цены |
| Абонемент МУЛЬТИКАРТА 12 месяцев | 629 | full-абонемент, часть может требовать восстановления цены |
| Абонемент Неделя Фитнес | 166 | нулевой trial/fallback, возможно нужно чистить способ оплаты |
| Абонемент НЕДЕЛЯ ФИТНЕСА БЕСПЛАТНО | 163 | нулевой trial/fallback, возможно нужно чистить способ оплаты |
| Абонемент Неделя сайт 2023 | 114 | нулевой trial/fallback, возможно нужно чистить способ оплаты |

Главная проблема:

`payment_type` может быть ложным, потому что при `price=0` он часто подтянут
fallback-логикой от соседнего платежа клиента. Для таких строк нужно понять:

- очищать ли `payment_type`, если нет direct-платежа;
- восстанавливать ли цену из `matched_payment_amount`, если direct-платеж есть.

### 4. price>0 + пустой payment_type + direct-платеж найден, raw method пустой

Количество: `2 941`.

Что в данных:

- `price > 0`;
- direct-платеж найден во всех строках;
- `matched_payment_operation = Оплата от клиента`;
- `matched_payment_amount` заполнен;
- `matched_payment_method` пустой.

Топ названий:

| contract_name | rows | что проверить |
|---|---:|---|
| Абонемент УЛЬТРА 12 месяцев | 1 616 | старый массовый слой 2019-2021, платеж есть, method пустой |
| Абонемент МУЛЬТИКАРТА 12 месяцев | 463 | старый массовый слой 2019-2021, платеж есть, method пустой |
| Абонемент НЕДЕЛЯ САЙТ | 284 | платный недельный слой, method пустой |
| Абонемент УЛЬТРА 1 МЕСЯЦ БЕЗЛИМИТ | 172 | платеж есть, method пустой |
| Абонемент УЛЬТРА 12 месяцев ДВОЙНОЕ / СЕМЕЙНЫЙ | 150 | платеж есть, method пустой |
| Абонемент МУЛЬТИКАРТА 12 месяцев ДВОЙНОЕ / СЕМЕЙНЫЙ | 75 | платеж есть, method пустой |

Сдвиг даты найденного платежа относительно `payment_date`:

| delta | rows |
|---|---:|
| 0 days | 2 897 |
| +-1..3 days | 30 |
| +-4..7 days | 9 |
| +-8..14 days | 2 |
| >14 days | 3 |

Главная проблема:

Связь с платежом надежная, но способ оплаты пустой в самом платежном документе.
Нужно найти альтернативный источник способа оплаты для старых direct-платежей
или получить бизнес-default для таких случаев.

## Текущий приоритет после deep-dive

1. Получить бизнес-ответ по `2` строкам `Абонемент Неделя Фитнес`: целевая
   строка абонемента `0`, но payment относится к строке `Доплата`.
2. Отдельно решить `15` historical full-строк без `Document131` refund:
   у них `price=0`, direct-платеж и заполненный `type_of_payment`, но источник
   служебности/возврата не найден.
3. Отдельно решить активные `44` full-строки: они не похожи на бесплатные или
   возвратные операции, у всех есть положительная строка продажи/direct payment;
   вероятно, там нужно восстанавливать `price`, а не очищать способ оплаты.
