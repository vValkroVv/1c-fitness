# Пересборка для заказчика на 22 сентября 2026

Пользователь запросил тот же состав и правила, что в
`output/20260921_fitbase_for_customer/`, с актуальностью на 22 сентября:
ровно 48 часов после окончания исходного backup.

- Backup: `data/Fitnes-20-09-26.fast.bak`.
- Фактическое окончание backup: `2026-09-20 20:12:12`.
- Новый единый срез: `2026-09-22 20:12:12`.
- Сентябрьская база: `FitnessRestored_20260630_macos`.
- Июньская база сохранена: `FitnessRestored_20260630_original`.
- Прогон: `20260922_final`.
- Полная служебная поставка: `output/20260922_final_delivery/`.
- Комплект заказчику: `output/20260922_fitbase_for_customer/` и одноимённый ZIP.
- Состав комплекта: семь XLSX и один ZIP фотографий, всего восемь файлов.

Прежние поставки сохраняются. Исходные даты операций, июньские правила
импорта, менеджеры, шаблоны и исключения остаются прежними. Дополнительная
история 766 клиентов в комплект заказчику не входит. По договорам 154220
и 144499 новые решения не получены; пересборка сохраняет прежние правила.

## Выполнение

Docker Desktop был выключен. Он запущен, затем запущен существующий
контейнер `mssql-fitness-2022`. Для Python SQL используется проверенный
локальный мост `scripts/docker_sql_bridge.py`, порт 11435.
Повторное восстановление баз не требуется.

Команда полного прогона:

```bash
python3 scripts/run_fitbase_migration.py \
  --backup-file data/Fitnes-20-09-26.fast.bak \
  --backup-sql-path /backup/Fitnes-20-09-26.fast.bak \
  --database FitnessRestored_20260630_macos \
  --run-name 20260922_final \
  --effective-offset-days 2 --port 11435 --use-existing
```

Общий лог: `logs/20260922_final_run.log`.

## Контроль результата

После полного прогона проверяются единый срез во всех слоях, сохранённые
XLSX, финансы, фотографии и изменения относительно комплекта 21 сентября.
Каждое отличие должно объясняться новым срезом; структура и бизнес-правила
сохраняются. Комплект заказчику собирается только из проверенной поставки
с READY/PASS, с проверкой состава, SHA-256 и CRC.

## Завершение полного прогона

Все 17 этапов завершены 21 сентября в 18:36:00 по часам Mac.
`READY.txt` и `reports/delivery_manifest.json` созданы с PASS.
Повторные HEADERONLY, FILELISTONLY, VERIFYONLY, SHA-256 исходного backup
и проверка UUID восстановления прошли успешно.

Независимая финансовая сверка на новом срезе проверила 104 787 фактов:
расхождений по числу движений, начислениям, оплатам и знаковому долгу нет.
Отдельный SQL-аудит дат и остатков завершился PASS: проверены 51 926 фактов
услуг и 1 146 ограниченных абонементов; исходные даты операций сохранены,
будущие операции исключены. Доказательства находятся в
`output/20260922_financial_audit/` и `output/20260922_boundary_audit/`.

Для повторного применения прежних независимых аудиторов добавлены параметры
ожидаемого среза и отдельных путей отчётов. Производственные SQL, сборщики
и бизнес-настройки не менялись. Прежние отчёты не перезаписывались.

Просмотрены 11 изображений контрольных диапазонов всех семи XLSX,
отрисованных Artifact Tool. Принятый формат импорта сохранён, включая
прежние узкие столбцы и видимое сокращение длинных подписей. Полнота значений
проверяется отдельно по содержимому ячеек; ширины столбцов не изменяются
в рамках запроса на идентичный формат.

Итоговый статус: полный pipeline, независимые SQL-проверки, сверка всех
сохранённых ячеек и упаковка компактного комплекта завершены с PASS.

## Сравнение с 21 сентября

Все 74 453 исходных ID клиентов сохранены. В owner-выборке 38 клиентов
перешли из действующих в реактивацию после окончания абонементов 21 сентября.
После прежнего удаления дублей в передаваемых заявках таких клиентов 36.
Меняются только два столбца этих заявок: `funnel` и `funnel_step`.
Соответствующие 36 строк исключены из файла карт действующих клиентов.

Все ячейки пяти остальных XLSX совпали с комплектом 21 сентября:
абонементы, их шаблоны, услуги, их шаблоны и отдельный договор 151350.
В том числе сохранены цены, оплаты, долги, даты, владельцы и остатки.
По фото сохраняются 35 422 ID, имени файла, телефона и SHA-256 изображения;
меняется только воронка 35 клиентов во внутреннем manifest.

При строгом сравнении TSV выявлены четыре отличия технического
`matched_payment_ref`: договоры 145146, 117131, 138278 и 128916.
В SQL используется `TOP (1)` с сортировкой по приоритету связи, методу,
дате и сумме, без последней сортировки по ID при полном равенстве кандидатов.
Для каждой пары оба документа оплаты присутствуют в обеих исходных
CSV-выгрузках; все их поля, кроме самого ID, совпадают. Остальные финансовые
поля фактов и все ячейки передаваемых договоров совпадают точно.
Это техническая неоднозначность выбора равного кандидата без влияния
на передаваемые значения; производственная логика сохранена.

Первый строгий протокол сохранён как
`output/20260922_cutoff_shift_audit/initial_strict_comparison.json`.
Аудитор дополнен проверкой обоих кандидатов по всем полям исходных CSV:
отличие ID допускается только после подтверждения их полного равенства
в обеих выгрузках. Данные поставки при этом не менялись.

## Итоговая упаковка

Независимая проверка XLSX: 67/67 PASS. Сравнение 21/22 сентября с проверкой
равных кандидатов оплаты: 42/42 PASS. Независимая проверка всех фотографий:
PASS. При упаковке проверены все восемь файлов в папке и ZIP, их размеры,
SHA-256 и CRC. Прежний архив также сохранил исходный SHA-256.

Готовый архив: `output/20260922_fitbase_for_customer.zip`, 862 910 800 байт.
SHA-256: `23482b1258b6670ba5b257bfec4c946b8108b64bb65ebcb501493cfa103c9572`.
Состав, сравнение и ссылки: [комплект заказчику](20260922_customer_files_only.md).
Временный SQL-мост остановлен после завершения проверок, порт 11435 свободен.
Обе базы проверены: ONLINE, MULTI_USER; контейнер сохранён.
Протоколы: `logs/20260922_database_states.txt` и
`logs/20260922_sql_bridge_stopped.txt`. Коммиты и push не выполнялись.
