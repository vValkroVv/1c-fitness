## Подключение к серверу

Перед подключением должен быть активен VPN `fitness`, а маршрут до сервера должен идти через него.

```sh
set -a; . ./.env; set +a
ssh "$LOGIN@$IP"
```

Когда SSH попросит пароль, используй значение `PASS` из `.env`.
